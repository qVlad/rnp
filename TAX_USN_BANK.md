# УСН-Доходы 6% (без НДС) — расчёт по методике бухгалтера

Воспроизводит расчёт из файла «Стас_Разметка_банка_с_01_01_2026_УСН_Доходы.xlsx», лист «Итоги», строки 13-20.

См. также: [`TAX_AUSN_BANK.md`](TAX_AUSN_BANK.md) — АУСН 8% по тому же кабинету.

## Где смотреть

Три страницы для разных налоговых конфигураций:
- **`/tax-report-usn`** — УСН 6% без НДС (для оборота ≤ 60 млн ₽/год)
- **`/tax-report-usn-vat5`** — УСН 6% + невозвратный НДС 5% (60–250 млн ₽/год)
- **`/tax-report-usn-vat7`** — УСН 6% + невозвратный НДС 7% (250–450 млн ₽/год)

Все три видны для `director` / `head_of_sales`.

- **API**: `GET /api/tax-report/usn?from=YYYY-MM-DD&to=YYYY-MM-DD&tax_rate=6&vat_rate=0|5|7`
- **Сервис**: [`backend/app/services/tax_report_usn.py`](backend/app/services/tax_report_usn.py)

## Режим с невозвратным НДС (5% / 7%)

С 2025 года (176-ФЗ от 12.07.2024) селлеры на УСН с оборотом более 60 млн ₽/год обязаны платить НДС. Ставка выбирается на 3 года:
- **5%** — для оборотов 60–250 млн ₽/год
- **7%** — для оборотов 250–450 млн ₽/год
- **20%** — для оборотов > 450 млн ₽/год (стандартная)

«Невозвратный» = нельзя вычесть входящий НДС, только начислить выходящий.

### Формула

```
База_gross = Отчёты_реализации + Тов_компенсация + Банк_выкупы + УПД_доставки + Возвраты_выкупы
НДС        = База_gross × ставка_НДС / (100 + ставка_НДС)
База_УСН   = База_gross − НДС
УСН        = База_УСН × ставка_УСН (6%)
Итого      = УСН + НДС
```

### Пример (Jan-Apr 2026)

| Режим | Base gross | НДС | Net база | УСН (6%) | **Итого нагрузка** |
|---|---:|---:|---:|---:|---:|
| Без НДС | 15 260 186.51 | 0 | 15 260 186.51 | 915 611 | **915 611** |
| + НДС 5% | 15 260 186.51 | 726 676 | 14 533 511 | 872 011 | **1 598 686** |
| + НДС 7% | 15 260 186.51 | 998 330 | 14 261 857 | 855 711 | **1 854 041** |

Разница: +НДС 5% даёт +75% к налогу vs «без НДС», +НДС 7% — +102%. УСН-часть слегка меньше (потому что net-база меньше gross), но добавление НДС полностью эту экономию перекрывает.

## Методика

```
База_месяц = Отчёты_реализации + Тов_компенсация + Банк_выкупы
           + УПД_доставки + Возвраты_выкупы
УСН        = База × 6%
```

Гибридная: для «Основной» отчётов — sale value (accrual), для «По выкупам» — cash. Это отличие от АУСН где «Основной» считается по cash (Bank).

### Источники и правила бакетирования

| Поле | Откуда | Бакет по месяцам |
|---|---|---|
| **G — Отчёты реализации** | `wb_report_detail.retail_amount` net (Продажа − Возврат) | `report_date_to` (period_end) |
| **Y — Товарная компенсация** | `wb_report_detail.ppvz_for_pay` для `Добровольная компенсация при возврате` | `wb_payment_order.paid_dt` («Основной») |
| **T — Банк выкупы** | `wb_payment_order.amount` (для «По выкупам» оплаченных) | `paid_dt` |
| **Z — УПД доставки** | `wb_payment_order.upd_delivery_amount` (импорт XLSX) | `report_date_from` (period_start) |
| **AA — Возвраты выкупы** | `wb_payment_order.buyout_returns_amount` (импорт XLSX) | `report_date_from` (period_start) |

«По выкупам» определяется через присутствие `realization_id` в `wb_redeem_notification.notification_number`.

### Сравнение с АУСН

| Компонент | АУСН (8%) | УСН (6%) |
|---|---|---|
| Sale_realization (Основной) | в Bank (T) + ВЗЗ (X) | **G напрямую** |
| Tovar_compensation (Y) | в ВЗЗ | **отдельной строкой по paid_dt** |
| Bank_buyout (По выкупам T) | в общем Bank | **отдельно по paid_dt** |
| УПД доставки (Z) | по period_end | **по period_start** |
| Возвраты выкупы (AA) | исключено (справочно) | **включено по period_start** |
| Ставка | 8% | 6% |

## Точность (наш dataset Jan-Apr 2026)

| Месяц | G | Y | T | Z | AA | База | Налог | Δ |
|---|---:|---:|---:|---:|---:|---:|---:|:-:|
| Январь | 1 915 479.60 | 0 | 30 166.13 | 3 913.57 | 7 500.08 | 1 957 059.38 | 117 423.56 | **+11 147** ⚠️ |
| Февраль | 2 133 105.41 | 3 222.28 | 30 888.12 | 3 692.57 | 0 | 2 170 908.38 | 130 254.50 | **0** ✅ |
| Март | 5 051 946.16 | 3 222.28 | 49 771.08 | 4 672.16 | 3 079.35 | 5 112 691.03 | 306 761.46 | **0** ✅ |
| Апрель | 5 978 591.79 | 0 | 34 319.81 | 3 624.09 | 2 992.03 | 6 019 527.72 | 361 171.66 | **0** ✅ |

**Δ Январь +11 147 ₽** (если не помечен флагом): отчёт `572437010` («По выкупам» 12-15..12-21 paid 01-12, T=11 147 ₽) — фискально-годовой переход. Бухгалтер исключает его из УСН (доход 2025 года), но ВКЛЮЧАЕТ в АУСН (cash-basis 2026). На странице УСН отметь чекбокс **«Исключить из УСН»** для этого отчёта → Δ станет 0₽.

## Manual override (флаг «исключить из базы»)

В таблице «Заявки WB» внизу страницы — чекбокс per-regime. См. [TAX_BOOKKEEPER_OVERRIDES.md](TAX_BOOKKEEPER_OVERRIDES.md).

## Миграции

| Ревизия | Что |
|---|---|
| 0024 | `wb_payment_order` базовая |
| 0025 | + `period_end`, `report_type`, `upd_delivery_amount` |
| 0026 | + `buyout_returns_amount` (для AA-колонки УСН) |

## Что отличается между АУСН и УСН-форматом xlsx бухгалтера

Если бухгалтер ведёт обе таблицы — они на одном dataset, отличаются только листами:
- **АУСН**: лист `Отчеты+УПД АУСН` + Итоги rows 1-9
- **УСН**: лист `Отчеты+УПД УСН` + Итоги rows 13-20

Импорт идёт по обоим листам одинаково (структура колонок A-AA идентична). Сервис автоматически бакетирует по правилам соответствующего режима.

## TODO

1. Импортёр UI-side: сейчас Стас файл импортируется одноразовым SQL-скриптом. Подключить как 15-ю сущность в `services/excel_io.py` с round-trip.
2. Bookkeeper-override flag: для январских переходов добавить поле `excluded_from_year` на `wb_payment_order` чтобы воспроизводить manual overrides.
3. Альтернативный режим «Доходы−Расходы 15%»: текущий сервис только для «Доходы 6%». Расходы потребуют учёта COGS и комиссий по правилам УСН-доход-расход.
