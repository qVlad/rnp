# RECON_GUIDE — сверка цифр РНП ↔ WB ЛК

> Версия 1.0 от 2026-05-26.
> Методология — `TrueStats art.74754` (наш diff: [`agents/references/truestats-article-74754-diff-2026-05-26.md`](agents/references/truestats-article-74754-diff-2026-05-26.md)).
> Прецедент-сверка: [`agents/references/feedback-reviews/recon-truestats-74754-vs-vipryn-2026-05-26.md`](agents/references/feedback-reviews/recon-truestats-74754-vs-vipryn-2026-05-26.md) — неделя 2026-05-18..24 на кабинете `vipryn@gmail.com`, 11/13 метрик сошлись рубль-в-рубль.

---

## Зачем сверять

WB периодически меняет финансовую отчётность задним числом (правки kvw, перевыставление документов, корректировки компенсаций). Если РНП показывает «прибыль 1.2M ₽», а в ЛК WB видно «к перечислению 1.05M ₽» — нужно знать **точно где разошлось** и **почему**.

«Просто гляну на дашборд» не работает: 17 разных метрик WB складываются в P&L нелинейно, без покомпонентной сверки baseline неизвестен.

## Когда сверять

- **Раз в месяц** для закрытых недель (профилактика — что финотчёты WB не «расползлись»).
- **При подозрении** (manager пишет «у меня в ЛК WB цифры другие» — открыть сверку).
- **Перед налоговой отчётностью** (АУСН/УСН — наш `tax_report` берёт данные из тех же `wb_report_detail`, расхождение в финотчёте → расхождение в декларации).

## Как сверять — 17 правил

> Все примеры — на неделе **2026-05-18..24**, кабинет `vipryn@gmail.com`.

### Правило 1 — Сумма продаж

| Источник | Формула | Значение |
|---|---|---|
| WB ЛК | *Дашборд → Продажи* (виджет за период) | сумма по неделе |
| WB xlsx | Σ «К перечислению Продавцу...» где `Тип документа=Продажа AND Обоснование=Продажа` | 1 347 388.38 ₽ |
| РНП | `/pnl?from=2026-05-18&to=2026-05-24&mode=final` → «Выручка нетто» | то же |
| Формула в БД | `Σ ppvz_for_pay WHERE supplier_oper_name='Продажа' AND doc_type_name='Продажа'` | |

✅ **Статус:** совпадает рубль-в-рубль.

### Правило 2 — К перечислению

`Сумма продаж − Сумма возвратов` (обе колонки «К перечислению Продавцу»).

| Источник | Значение |
|---|---|
| WB ЛК | *Финансы → По неделям* — колонка «К перечислению» | 1 209 641.07 ₽ |
| РНП | `/pnl-reconciliation` → строка «К перечислению (WB)» | 1 209 641.07 ₽ |

✅

### Правило 3 — Логистика

Σ «Услуги по доставке товара покупателю» из обоих отчётов.

| Источник | Значение |
|---|---|
| РНП `/pnl` | строка «Логистика WB» | 287 809.24 ₽ |
| БД | `Σ delivery_rub WHERE realization_id=N` | то же |

✅

### Правило 4 — Хранение

В РНП и в ЛК WB должно сходиться, **но WB-источник — отдельный**: *Аналитика → Отчёты → Платное хранение* (не еженедельный финотчёт). Сверять через `/pnl` колонку «Хранение WB».

| Источник | Значение (пример) |
|---|---|
| WB ЛК → Аналитика → Платное хранение | 25 812.76 ₽ |
| РНП `/pnl` | 25 812.72 ₽ |

✅ (Δ 0.04 ₽ — округление, норма)

### Правило 5 — Платная приёмка

*Аналитика → Отчёты → Операции при приемке*. **Edge case TrueStats:** отгрузка в воскресенье часть оплаты падает в воскресенье, часть в понедельник → могут не сходиться неделя-к-неделе, но за месяц-два выравниваются.

| Источник | Значение |
|---|---|
| WB ЛК | 0 ₽ |
| РНП | 0 ₽ |

✅

### Правило 6 — Количество продаж

`qty(Продажа) − qty(Возврат)`. Считаем штуки, не суммы.

| Источник | Значение |
|---|---|
| xlsx | 392 − 42 = 350 шт |
| РНП `/units` (для всех SKU за период) | 350 шт |

✅

### Правило 7 — Штрафы

Σ «Общая сумма штрафов» из обоих отчётов.

| Источник | Значение |
|---|---|
| РНП `/pnl` → «Штрафы» | 0 ₽ (для этой недели) |

✅

### Правило 8 — Прочие удержания ⚠️

**Формула TrueStats** — это **4 компонента − 14 исключений**:

**4 компонента (плюс):**
- Разовое изменение срока перечисления
- Стоимость участия в программе лояльности
- Удержание за баллы программы лояльности
- Прочие удержания из фин-отчёта (где `Обоснование=Удержание`)

**14 категорий исключений (минус):**
- ВБ.Продвижение (реклама)
- ВБ.Медиа
- Погашение займов
- Хранение (считается отдельно через правило 4)
- … (полный список в `agents/references/truestats-article-74754-diff-2026-05-26.md`)

⚠️ **Внимание:** В РНП **до TASK-LEAD-135** мы показываем raw поле `deduction` без декомпозиции. Это **совпадёт** с TrueStats только если в `deduction` у тебя нет рекламы/займов. **Для проверки** — в WB-xlsx сложи 4 компонента и вычти 14 исключений вручную.

После TASK-LEAD-135 — будет авто.

### Правило 9 — Рекламные расходы

**Не из финотчёта!** TS подчёркивает: «еженедельный отчёт удерживает рекламу за предыдущий период». Брать из:
- WB ЛК: *Продвижение → Финансы* — «фактические расходы за период»
- РНП: `/ads-heatmap` или `/external-marketing` для ВБ.Медиа

### Правило 10-11 — Заказы (кол-во и сумма) — ⛔ СКРЫТО из автосверки (TASK-LEAD-150)

**Методика TrueStats** (для справки): источник зависит от даты подключения магазина —
- **До даты подключения − 7 дней**: *Аналитика → Лента заказов* (сырые orders)
- **После** (свежие недели): *Аналитика → Воронка продаж* (агрегаты по карточкам)
- Edge case: если период пересекает дату−7, разделить и сверять обоими методами.

**Почему скрыто из `/reconciliation-auto`:** РНП хранит заказы из **Statistics
API** `/api/v1/supplier/orders`, который **по дизайну WB не отдаёт заказы в
рассрочку** («Оплата частями») и сам WB-справочник помечает «для сверок не
использовать». TrueStats же сверяет заказы с **аналитикой WB** (Воронка/Лента),
которая считает ВСЕ заказы. Поэтому наш источник систематически ниже дашборда
на долю рассрочки (~16%) — Δ→0 недостижима by design.

Замер на проде (кабинет vipryn / tenant 1, неделя 18-24.05.2026):

| Источник | Значение |
|---|---|
| WB ЛК **Воронка** (аналитика) | ~951 заказов / 5 352 325 ₽ |
| WB ЛК **Лента** (аналитика, по дате оформления) | 948 / 5 336 778 ₽ |
| РНП `wb_orders` (Statistics API, без рассрочки) | **798 / 4 458 020 ₽** |
| Разрыв | **−150 / −878 758 ₽** ≈ доля рассрочки |

Обе аналитики WB согласованы (~950) — выбивается именно Statistics API. Это
**не баг синка** (чекпоинт свежий, распределение по дням ровное). Подтверждение:
«Кол-во проданных шт.» (правило 6, из отчёта реализации) сходится в ноль.
Деньги (правила 1-9, 12-17) сверяются из отчёта реализации отдельно, Δ=0.
См. `WB_API_REFERENCE.md` § `/supplier/orders`.

### Правило 12 — Реализация

`Σ «Цена розничная с учетом согласованной скидки» (Продажа) − Σ (Возврат)`.

| Источник | Значение |
|---|---|
| РНП `/pnl` final → «Выручка» (брутто) | 1 917 094.22 ₽ |

✅

### Правило 13 — Комиссия (общая)

`Реализация − К перечислению`. Это эффективная комиссия WB.

| Источник | Значение |
|---|---|
| РНП `/pnl` → «Комиссия WB» | 707 453.15 ₽ |

✅

### Правило 14 — Номинальная комиссия МП ⚠️

**Формула TrueStats:** `Σ (Цена розничная × Размер кВВ %/100) (Продажа − Возврат)`. Это «по тарифу WB», в отличие от эффективной (правило 13).

Зачем сверять отдельно: если WB **задним числом меняет кВВ** (бывает раз в 1-2 месяца), эффективная комиссия (13) тоже едет — но **тариф (14)** сразу покажет «вот тут поменяли».

⚠️ **Внимание:** В РНП **до TASK-LEAD-132** наша колонка `kvw` хранит не то значение, что в xlsx «Размер кВВ, %». Сейчас наша «Номинальная комиссия» = **121 207 ₽**, в xlsx — **647 019 ₽** (8× меньше). Считать вручную через xlsx до фикса.

### Правило 15 — СПП ⚠️

**Формула TrueStats:** `Σ (Цена розничная − «Вайлдберриз реализовал Товар (Пр)») (Продажа − Возврат)`. Сравнить со строкой «Скидка МП» в отчёте.

⚠️ **Внимание:** В РНП **до TASK-LEAD-133** наш `ppvz_vw` это не то поле что нужно (он = «Вознаграждение Вайлдберриз без НДС»). Наша СПП = **1 761 961 ₽**, xlsx — **420 939 ₽** (4× больше). Считать вручную.

### Правило 16 — Эквайринг ⚠️

`Σ «Эквайринг» для Продажа − Σ для Возврат` (split, не общий sum).

⚠️ **Внимание:** В РНП **до TASK-LEAD-134** наша колонка `acquiring` в `/pnl-reconciliation` это **общий SUM по всем строкам** (включая логистические). Завышение **+14 146 ₽** на этой неделе. После фикса — split.

| Источник | По TS-формуле | Наш расчёт сейчас |
|---|---|---|
| xlsx | 60 433.38 ₽ | — |
| РНП | — | 74 579.64 ₽ |

### Правило 17 — Компенсации

**3 этапа TrueStats:**
1. Σ для 3 типов: Компенсация подмененного товара / Возмещение издержек / Оплата/частичная компенсация брака (столбец «К перечислению»)
2. + Σ для 3 типов: Оплата потерянного товара / Компенсация ущерба / Добровольная компенсация (Продажа)
3. − Σ те же 3 типа (Возврат)

⚠️ Не реализовано в РНП (TASK-LEAD-136). В большинстве недель = 0₽, для проверки — вручную через xlsx.

---

## Чек-лист «всё сошлось»

После сверки 17 пунктов — у тебя есть **3 типа результата** на каждый:

| Тип | Что означает |
|---|---|
| ✅ Δ < 1 ₽ | Округление, норма |
| ⚠️ 1 ₽ < Δ < 100 ₽ | Подозрительно. Проверить: правильная ли неделя, нет ли исключённых отчётов (TASK-LEAD-040 bookkeeper overrides), не ли отсечён ли SKU brand-фильтром |
| 🔴 Δ > 100 ₽ | Реальное расхождение. Сценарии: WB прислал обновление финотчёта задним числом (наша sync не подтянулся ещё — см. TASK-LEAD-131), мы маппим не то поле (TS gap), или баг в формуле |

## Автоматизация сверки — roadmap

- **TASK-LEAD-137** — страница `/reconciliation-auto`: 17 метрик нашей БД vs ручной ввод/xlsx-upload, цветовая Δ
- **TASK-LEAD-138** — Chrome-extension автозагружает финотчёт из ЛК WB → `/reconciliation-auto` колонка автозаполняется без действий
- **TASK-LEAD-132..136** — фикс methodology gaps (kvw / СПП / Эквайринг / Прочие удержания / Компенсации)

После всего этого ручная сверка станет не нужна — РНП сам покажет «всё OK» или «вот тут расхождение».

---

## FAQ

**Q. Цифры в РНП отличаются от ЛК WB. С чего начать?**
A. Сначала проверь правило **1** (сумма продаж). Если сходится — переходи к правилам 12-13. Если не сходится правило 1 — проверь sync `report_detail`: `/settings` → таблица sync-checkpoints → дата последнего обновления.

**Q. Через сколько часов после закрытия недели WB готов сверять?**
A. WB генерирует еженедельный финотчёт **в понедельник** (обычно к 12:00 МСК, иногда до вторника утра). Наш beat `sync_report_detail_daily` в 04:15 МСК — если WB опоздал, утренний sync ничего не подтянет, следующий — через сутки. Manual trigger в `/settings` («Отчёт реализации за 2 недели») вытянет.

**Q. Менеджер видит цифры в P&L отличные от ЛК WB. Что не так?**
A. Manager в РНП живёт в **brand-scope** (фильтр по своим `brand_assignments`), ЛК WB не фильтрует. Если бренды менеджера ≠ все бренды кабинета — расхождение нормально. Сверять надо в скоупе менеджера (по xlsx можно фильтровать вручную по бренду через колонку E).

**Q. Reporting-mode operational vs financial — какой использовать для сверки?**
A. **Operational** (default) — группирует по `sale_dt` (день выкупа), как WB-дашборд. **Financial** — по `rr_dt` (день платёжки), как «WB Финансы → Реализация». TrueStats методология (17 правил) ближе к **financial**, потому что считает по rr_dt-строкам финотчёта. Если в РНП включить financial-режим в Layout footer → цифры точнее матчатся с правилами TS.

---

## Связанные документы

- [`agents/references/truestats-article-74754-diff-2026-05-26.md`](agents/references/truestats-article-74754-diff-2026-05-26.md) — diff методологии TS vs нашей выжимки vs наш код
- [`agents/references/feedback-reviews/recon-truestats-74754-vs-vipryn-2026-05-26.md`](agents/references/feedback-reviews/recon-truestats-74754-vs-vipryn-2026-05-26.md) — прецедент-сверка
- [`TRUESTATS_REFERENCE.md`](TRUESTATS_REFERENCE.md) §18.5 — устаревшая выжимка (8 пунктов вместо 17, нужно обновить)
- [`TAX_AUSN_BANK.md`](TAX_AUSN_BANK.md), [`TAX_USN_BANK.md`](TAX_USN_BANK.md) — налоговая база использует те же `wb_report_detail` строки, что и P&L → расхождение в сверке = расхождение в декларации
