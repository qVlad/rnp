# Feedback-review — раунд 13 (2026-05-25)

Post-feature review (правило 2.5) для пакета фич v0.30 → v0.34 (спринты P1
UX polish + P2 + P3 + sub-agent работы).

**Входы:**
- `persona-reports/round-13-qa-seller-2026-05-25.md` (sub-agent U1: QA + UX-as-seller)
- `persona-reports/round-13-rop-manager-2026-05-25.md` (sub-agent U2: UX-as-rop + UX-as-manager)

**Synthesizer:** Product Strategist + Lead + PM в main session (Claude Opus 4.7).

---

## Главные находки

### 🔴 НОВЫЙ КРИТИЧЕСКИЙ БАГ

**BUG-DEV-014** (P1): `GET /api/dashboard/kpi-breakdown` не принимает `reporting_mode` query-param и хардкодит `sale_dt_filter()` (operational). При переключении toggle в financial — Dashboard KPI идут по `rr_dt`, breakdown popup по тем же KPI — по `sale_dt`. **Σ breakdown ≠ KPI** на возвратах (расхождение 1-2 недели). Все остальные `/api/dashboard/*` endpoints `reporting_mode` принимают.

### Half-features + регрессии от спринта

1. **TASK-LEAD-066 half-feature:** клик на breakdown row идёт на `/units?nm_id=X`, но Units.tsx не фильтрует по `?nm_id`. Менеджер кликает «#12345» → попадает в полный список → ищет руками. Drill оборвётся на полпути.
2. **Badge financial mode только на Dashboard + PnL** (TASK-LEAD-060): на /units, /weekly-report, /pnl-reconciliation цифры тоже зависят от mode, но badge'а нет.
3. **TransitCalculator multi-warehouse использует один тариф** для всех candidate-складов. Если у extension есть per-pair тарифы — должны использоваться.
4. **TG-share manager → self** концептуально странно. Менеджер думает «отправь РОПу», получает у себя в личке.

### Stale статусы в `tasks-lead.md` (admin-fix)

- TASK-LEAD-065 — Открыта → должна быть **Выполнено** (by_brand UI в v0.33.0)
- TASK-LEAD-067 — Открыта → **Выполнено** (2-col layout в v0.33.0)
- TASK-LEAD-068 — Открыта → **Выполнено** (multi-warehouse compare в v0.33.0)
- TASK-LEAD-069 — Открыта → **Выполнено** (ReconciliationHero polish в v0.33.0)

### Coverage gaps из раунда 12 — оставшиеся

- **BUG-UI-006** (WeekProfitHero показан manager'у без role-guard) — был P3, в спринт не вошёл. Production-кабинет до сих пор смешивает термины.
- **HYP-001** (composite «State of Business» вместо 4 hero-виджетов) — теперь **прагматично необходимая**: 6+ heros на топе Dashboard'а занимают 70-80% viewport на laptop.

---

## Классификация feedback'а

### BUG (исправить)

| Code | Где | Что | Severity |
|---|---|---|---|
| **BUG-DEV-014** | `backend/app/api/dashboard.py:122-142` + `services/kpi_breakdown.py:130` | `/kpi-breakdown` не принимает reporting_mode, хардкодит sale_dt_filter → Σ breakdown ≠ KPI в financial | **P1** |
| BUG-DEV-015 | `backend/app/api/transit_tariffs.py:134-242` + extension interceptor | Нет schema validation на upload (только `rate ≥ 0`). Если WB изменит формат — backend примет мусор. Нет audit source-URL. | P3 |
| BUG-DEV-016 | `frontend/src/pages/TransitCalculator.tsx:496-513` | Auto-fill overwrites manual edit при синке тарифа (`autoFilled === key` защищает только одну сессию). Юзер не видит что значение пропало. | P3 |
| BUG-UI-007 | `MetricBreakdownPopup.tsx:179-209` | Middle-click на row → Link открывает в новой вкладке + onClose закрывает popup. Дискомфорт. | P3 |

### TASK (новая работа в backlog)

**P1 — исправить сразу (блокирующие):**

| Task | Источник | Что |
|---|---|---|
| TASK-LEAD-080 | U1 QA | Закрыть BUG-DEV-014: `/api/dashboard/kpi-breakdown` принимает `reporting_mode`, передаёт в `compute_kpi_breakdown`, использует `get_period_filter()`. + unit-test «Σ breakdown ≈ KPI в обоих режимах». |
| TASK-LEAD-081 | U1 + U2 | Units.tsx читает `?nm_id` query-param → фильтрует таблицу + scroll к ряду. Досайка half-feature TASK-LEAD-066 (без этого breakdown drill бессмыслен). |

**P2 — следующий спринт:**

| Task | Источник | Что |
|---|---|---|
| TASK-LEAD-082 | U2 | Badge `ReportingModeBadge` на /units, /weekly-report, /pnl-reconciliation. Расширение TASK-LEAD-060 на все страницы где `useReportingMode()` влияет. |
| TASK-LEAD-083 | U2 | WeekProfitHero — role-guard для manager'а ИЛИ явная подпись «по твоим брендам» в header при `user.role === 'manager'`. Закрывает BUG-UI-006 из раунда 12. |
| TASK-LEAD-084 | U2 | TransitCalculator multi-warehouse compare использует per-pair `(hub × destination)` тариф из `wb_transit_tariff` для каждого candidate (не общий manual). |
| TASK-LEAD-085 | U2 | Localization by_brand — `wow_pct` колонка (за прошлую неделю с offset −7d) + `min_orders ≥ 10` threshold для фильтрации статистического шума. |
| TASK-LEAD-086 | U2 | WeeklyReport scoreboard manager_name → клик ведёт на /weekly-report с brand-filter этого менеджера. |

**P3 — долгий backlog:**

| Task | Источник | Что |
|---|---|---|
| TASK-LEAD-087 | U2 | WeeklyReport scoreboard pre-aggregation в Celery beat (таблица `manager_weekly_scoreboard`) — избежать N×compute_dashboard на тенантах с 10+ менеджерами. |
| TASK-LEAD-088 | U2 | Localization worst-SKU per-SKU recommendation backend (заменить tenant-wide эвристику на per-SKU buyer-cluster breakdown). |
| TASK-LEAD-089 | U2 | TG-share manager scenarios: либо `User.boss_id` для «отправить моему РОПу», либо явная подпись «Отправит тебе в личку — добавь РОПа в чат для broadcast». |
| TASK-LEAD-090 | U1 + U2 | Кастомный `<Dialog>` компонент для confirm в TG-share (вместо native `confirm()`). Mobile UX + brand consistency. |
| TASK-LEAD-091 | U2 | Layout: `/notifications` в РОП whitelist (alert rules = часть РОП workflow). |
| TASK-LEAD-092 | U2 | PromoCalculator: auto-suggest `boostPct` из истории прошлых акций тенанта (если есть данные); fallback на manual. |
| TASK-LEAD-093 | U1 | TransitCalculator wizard-mode: упрощённый вид «только SKU + хаб + конечный, всё остальное auto/default». Текущая форма 13 полей overload для разовой задачи. |
| TASK-LEAD-094 | U1 | TransitCalculator: badge «⚠ Тариф давно не обновлялся» если `synced_at > 30 дней назад`. |
| TASK-LEAD-095 | U1 | DocPage расширить whitelist: `/transit-calculator`, `/supply-calculator`, `/pnl-reconciliation` методички. Сейчас 2 slug'а. |
| TASK-LEAD-096 | U1 | ReconciliationHeroWidget: разделить «сходимость с WB» (Δ%/Δ₽) и «доля выплаты» — разные карточки или collapsible. На laptop карточка 30vh — overload. |
| TASK-LEAD-097 | U1 | WeekProfitHero «vs 4-week avg» tab — был обещан в TASK-LEAD-073 описании, не реализован. |

### HYP (для Product Strategist)

| HYP | Источник | Что проверить |
|---|---|---|
| **HYP-001 (повторно)** | U1 + U2 | Composite «State of Business» карточка вместо 4-6 hero-виджетов. После v0.34 на топе Dashboard'а: WeekProfitHero + ReconciliationHero (3-col) + TodayVsYesterdayStrip + WeeklyChangesFeed + AlertsBar + CustomMetricsCard + ManagerPlanProgressCard. **Прагматично необходимая**, не просто гипотеза. |
| HYP-004 | U2 | Per-brand weekly comment — manager пишет в свой бренд, РОП видит overall. Стоит ли усложнять или overall достаточно? |
| HYP-005 | U2 | WeeklyReport scoreboard drill-down: клик на менеджера → one-page «summary about Иванов» (top-5 problems + recs + KPI). |
| HYP-006 | U2 | TransitCalculator `weeks_window` selectbox (1-12) для suggest_units — сезонные товары. Стоит ли давать manager'у play с горизонтом? |

### Отброшено (с обоснованием)

| Что | Обоснование |
|---|---|
| PromoCalculator: разрешить discount=0 | Расчёт работает, но бессмысленный. Trivial edge, не блокер. |
| Multi-warehouse compare минимум 2 склада | Currently `≥1` достаточно. UX-нюанс, не баг. |
| Манагер видит multi-manager scoreboard | Не нужно. Менеджеру не интересно «как Петров живёт» — `canSeeScoreboard = director|head_of_sales` правильно. |
| PDF-кнопка для РОПа | Оставить рядом с TG-share. Offline-копия для встречи / распечатки. |
| Manual ввод тарифов транзита | Auto-fetch работает, но manual fallback оставить (тенанты без extension / без director-роли). |
| WeekProfitHero relative подсказка «закрыта 4 дня назад» | Cosmetic, «закрытая неделя» уже явный термин. |
| WoW vs 4-week avg в WeekProfitHero | Минор. Реализовать когда будет касание виджета. |
| `weeks=4` overhead в ReconciliationHero (использ. [0]) | Тривиальная экономия для single-tenant. |
| MetricBreakdownPopup: badge «откуда пришёл» на /units | Cosmetic. |
| PromoCalculator sticky-первая-колонка | Минор, реализуется при касании. |

### Admin-tasks (stale-cleanup)

| Task | Что |
|---|---|
| TASK-LEAD-098 | Update статусы 4 stale-задач в `tasks-lead.md` → Выполнено: TASK-LEAD-065/067/068/069. |

---

## Итог

| Категория | Количество |
|---|---|
| **BUG → исправить** | 4 (BUG-DEV-014 P1 + BUG-DEV-015 P3 + BUG-DEV-016 P3 + BUG-UI-007 P3) |
| **TASK → backlog** | 19 (TASK-LEAD-080..098) — 2 P1, 5 P2, 11 P3, 1 admin |
| **HYP → проверить** | 4 (HYP-001 повторно + HYP-004/005/006) |
| **Отброшено** | 10 |

**Distribution по приоритету:**
- P1 (этот спринт): 2 TASK + 1 BUG (kpi_breakdown reporting_mode + Units.tsx ?nm_id filter)
- P2 (следующий спринт): 5 TASK + 0 BUG (полировка после spec'а)
- P3 (долгий backlog): 11 TASK + 3 BUG (микро-улучшения + рефакторинг)

**Главные инсайты:**

1. **Spring P1+P2+P3 раунда 12 успешен на 95%.** Все 13 ключевых задач (LEAD-058..073) реализованы и в production. Качество — Working. Проблемы — точечные регрессии (kpi_breakdown reporting_mode, half-feature ?nm_id filter).

2. **Лучшая фича пакета — WeeklyReport scoreboard.** Превратил manager-digest в РОП-обзор за один спринт. Концепция и реализация — sound.

3. **TransitCalculator стал power-tool, но переусложнён** для разового use-case. 13 полей. Wizard-mode упрощённой формы — реальная потребность.

4. **Dashboard теперь перегружен** 6+ hero-виджетами. HYP-001 (composite карточка) теперь не «может быть полезно», а **прагматично необходимая**.

5. **DocPage запущен** но покрывает только 2 slug'а. Если позиционируется как user-guide — нужен ещё минимум 5 методичек.

---

**Подписи:** Product Strategist (классификация HYP + 5 главных инсайтов) + Lead (технический scope + BUG ↔ TASK границы + admin-cleanup 098) + PM (приоритезация P1/P2/P3) — все в main session Claude Opus 4.7, 2026-05-25.

Все TASK/BUG-записи в этом файле — резюме. Реальные записи заведены в:
- `agents/tasks-lead.md` — TASK-LEAD-080..098
- `agents/bugs-developer.md` — BUG-DEV-014/015/016
- `agents/bugs-design-engineer.md` — BUG-UI-007
