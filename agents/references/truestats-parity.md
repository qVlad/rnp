# TrueStats ↔ РНП — паритет данных (рабочие заметки)

> Цель: сверить наш сервис с TrueStats цифра-к-цифре по кабинету **Onyx** и
> устранить расхождения. Сессия 2026-06-04…05. Этот файл — чтобы вернуться к
> незакрытым пунктам (налоговая база, ДДС/План-факт, ad-bonus и т.п.).

## Доступ к TrueStats API (важно)

- Основной `api.truestats.ru` и `api3` — **мертвы/блокируют** датацентровый IP хоста.
- **Рабочий хост: `api2.truestats.ru`** (его использует зеркало). С хоста доступен.
- Фронт-зеркало: **`https://mirror-app.truestats.ru`** (когда `app.truestats.ru` лежит).
- Авторизация: заголовок `x-auth-token` (хранится в localStorage фронта, 120-hex).
  Креды для логина в зеркало: `altecomllc@yandex.ru` / `p3v-g46-4dD-Tgd`.
- Кабинет **Onyx = account id 25143**. Фильтр в теле: `filters:{accounts:[25143]}`.
- Ключевые эндпоинты:
  - `POST /reporting/main/stats` — плитки Оцифровки/Сводного (realisation, sales,
    toTransfer, commission, acquiring, logistics, storage, tax, taxBase,
    wbFinalReward, profit, profitWithoutExpense, advertisingExpenseSum, expense …).
  - `POST /reporting/main/products` — per-SKU (article, realisation, sales,
    cost, costOfSales, totalSales, profit, expense, advertisingExpenseSum …).
  - `POST /reporting/facets` — справочник accounts/brands/categories.
  - `POST /v1/operation/list` — реестр финопераций (доходы/расходы, OPEX).
  - `POST /v1/operation/filter-values`, `GET /v1/operation/bill/balance`.
  - `POST /v1/cashflow/payment-calendar` — ДДС/платёжный календарь.
  - `POST /plan-fact/list` — План-факт.

## Карта разделов (TS route → наш)

| TS раздел | TS route | Наш |
|---|---|---|
| Дашборд | / | / |
| Сводный отчёт | /week | /summary-report |
| Сводный по бизнесу | /pnl | /business-summary |
| Прочие удержания | /other-deductions | /deductions |
| План-факт | /plan-fact | /plans (TS-копия — TODO) |
| ДДС | /cash-flow | /cash-flow (TS-копия — TODO) |
| Операции | /operations-list | /operations |
| Дополнительно | /operations/category | /finance-extras |
| Себестоимость | /cost | /cost-history |
| Группы товаров | /groups | /product-groups |
| Склады | /warehouses | /stocks |
| Модуль РНП | /rnp | /rnp-module |
| Аналитика РК | /rk-efficiency | /ad-campaigns |
| Тепловая карта | /rnp/heat-map | /ads-heatmap |
| Настройки РНП | /rnp-settings | Excel-импорт Себестоимости + /settings |

## Результаты сверки (закрытая неделя 12–18.05, режим rr_dt)

**Сходится копейка-в-копейку:** реализация 2 254 937.08, продажи (после СПП)
1 739 796.19, возвраты 207 129.91, COGS 309 578, эквайринг 70 340.08,
логистика 304 178.17, хранение 27 138.05, штрафы 50; per-SKU (после фикса)
реализация/продажи/COGS/выкупы — тоже 1:1.

**Методика-ключ:** TS считает Реализацию/Продажи по **`rr_dt`** (дата отчёта),
реализация = `retail_price` (до СПП), продажи = `retail_amount` (после СПП).

## Исправленные ошибки (наши)
- `/deductions` «Прочие удержания» — был scope «все удержания» (441k), стал
  только non-core (штрафы/удержания/приёмка/доплаты) — как TS (v0.62.1).
- `/stocks` Склады — протух снапшот (20.05), перезапустил sync (→05.06). ⚠️ beat
  сток-синка не обновлял ~2 нед при живых orders/sales — **проверить расписание
  worker-stats** (TODO).
- `/summary-report` per-SKU — брал из /units (sale_dt) → расход +5%. Сделал
  отдельный `/api/summary-report` (rr_dt, retail_price, net-COGS) — 1:1 (v0.62.2-3).

## ⏳ Налоговая база (НЕ закрыто — решено оставить нашу)

- TS `taxBase` 1 735 304.31 vs наш `retail_amt_net` 1 739 796.19. **Δ 4 491.88 (0.26%)**.
- Ни одно поле TS Δ не объясняет. Гипотеза: TS считает базу по **sale_dt**
  (наш sale_dt-after-СПП = 1 735 018.87 ≈ taxBase, Δ 285). Не подтверждено.
- `compensation` 1 729.21 = наш зазор по «К перечислению» (TS добавляет
  компенсацию в `toTransfer`/ppvz; у нас её нет — мелочь, на страницах не видно).
- **Решение пользователя (2026-06-05): оставить нашу базу** (after-СПП, net по
  rr_dt — методически верна для АУСН «Доходы»). Вернуться, если понадобится 0.00.
- Где менять, если решим: `pnl_builder._compute_tax` (`cash_income=retail_amt_net`).

## OPEX из TS (заводим)

TS distribution: по **account (кабинет)** или **brand**, метод `equal`/`proportional`.
За май у Onyx (account 25143) targeted ровно 2 записи (`/v1/operation/list`):
- id 514049: **12 000**, «Аренда склада», 15.05, account→Onyx.
- id 514050: **75 000**, «ФОТ сотрудникам», 15.05, account→Onyx.
- Σ = **87 000** = TS `expense` Onyx ✅. Остальные 10 записей (419 440 всего) — на
  другие кабинеты/бренды (INCANTINA/Нетапки/LeymanKids/LeymanDress), к Onyx не относятся.
- Реклама у TS — **отдельное** поле (`advertisingExpenseSum`, не в `expense`).
  TS ad Onyx: 24 000 (нед) / 99 108 (май). Наша реальная WB-реклама 25 722/нед
  (в P&L уже как `ad_cost`). TS ad — ручная (ровные числа), отличается от WB-API.
- Заводим в наш `/opex`: «Аренда склада» (id 4) 12 000 + «Заработная плата»
  (id 1, = ФОТ) 75 000, дата 15.05. После этого прибыль Onyx сойдётся (−87 000).

## Остаточные расхождения прибыли (после OPEX) — ВАЖНО, не закрыто

OPEX заведён (2026-06-05): opex_operating Onyx 12-18.05 = 87 000, наша прибыль
стала **504 704.70**, TS profit **534 515.90** → **наша ниже на 29 811**.
- До OPEX наша прибыль была 591 705 (выше TS на 57 189). После −87 000 OPEX —
  ниже на 29 811. Значит **до-OPEX прибыль TS на ~29 811 ВЫШЕ нашей** при том,
  что реализация/продажи/COGS/комиссия(номинал)/эквайринг/логистика/хранение
  совпадают копейка-в-копейку. Источник Δ29 811 НЕ найден.
- Кандидаты на Δ29 811 (проверить): (а) СПП-компенсация (mpDiscount/
  wbCompensationAmount 515 141) — TS может частично возвращать в прибыль;
  (б) tax в profitWithoutExpense (включает/нет); (в) commission net vs nominal
  в формуле прибыли; (г) ad 24 000 vs 25 722.
- Реклама: ⚠️ **ИСПРАВЛЕНО ПОНИМАНИЕ** — TS рекламу НЕ вводит вручную, тянет из
  WB advert API (advertisingExpense, bonus=0). Наш зазор был из-за **бага синка**:
  `sync_ad_stats` удалял весь 60-дн диапазон и вставлял только успешные чанки →
  при WB 429 на свежем чанке данные рекламы стирались (регрессия: было до июня,
  стало max 06.05). Починено (v0.64.1): удаляем только fetched-даты. Данные
  восстановятся при успешном fetch (сейчас WB 429-throttle на этом продавце).
- Налог: Δ 0.26% (база, см. выше).
- **Δ29 811 разложен:** «Удержание» 26 000 (мы вычитаем, TS нет — мы консервативнее) +
  компенсация 1 729 (TS в toTransfer) + реклама 1 722 (из-за бага синка) + налог 360.
  В TS-копии `/summary-report` прибыль по формуле TS (без «Удержания», +OPEX-аллокация);
  наш P&L/сверка с WB — оставлены точными.

## Заказы / Прочие удержания / Прибыль — СВЕРЕНО с ЖИВЫМ TS 2026-06-05 (DEV-058)
Сверка: ЖИВОЙ прод (rnp.sellerfriends.ru, tenant «Default» = Onyx) vs ЖИВОЙ TS
(api2.truestats.ru, account **25143**, токен из localStorage `authToken`), период
18-24.05. ✅ Совпали «в рубль»: realisation 1 928 199.98, sales 1 505 573.92,
toTransfer 1 216 706.92, COGS 257 362, logistics 289 297.18, storage 25 812.7,
commission 288 867.

- **Прибыль — формула TS подтверждена ТОЧНО:** TS profit = toTransfer − logistics
  − storage − costOfSales − tax − advertising − **otherDeduction** =
  1 216 706.92 − 289 297.18 − 25 812.76 − 257 362 − 120 138.66 − 18 900 − 22 990
  = **482 206.32** (TS отдаёт ровно это). → `/summary-report` profit ОБЯЗАН
  вычитать прочие удержания (v0.64.7 было верно; мой ошибочный откат отменён в
  v0.64.8). С нашими ad 19 705 и tax 120 445: profit = 481 093 ≈ TS 482 206
  (Δ1113 = реклама 806 + налог 307). Штрафы (penalty) в прибыль НЕ входят.
- **Прочие удержания — остаток 6 902 РЕШЁН (bonus_type_name):** обе строки —
  операция «Удержание», но разные услуги: 05-21 = «Предоставление услуг по подписке
  **Джем**» (22 990 → TS относит к otherDeduction ✓), 05-24 = «Оказание услуг **WB
  Продвижение**» (6 902 → это РЕКЛАМА через финотчёт, TS НЕ относит к otherDeduction
  ✗). Фикс (v0.64.9): фильтр `bonus_type_name ILIKE '%продвиж%'` (`_PROMO_BONUS_LIKE`)
  исключает «WB Продвижение» из «Прочих удержаний» в `/summary-report` и
  `/deductions` (отдаём отдельно `promo_ad`/`promo_total`). → «Прочие удержания» =
  22 990 = TS точно; profit = 481 093 ≈ TS 482 206 (Δ1113 = ad 806 + tax 307).
  Примечание: TS «WB Продвижение» не учитывает в прибыли вовсе (его adv=18 900
  campaign-only) — мы тоже не дублируем (она в модуле Реклама-РНП / native P&L).
- **Заказы — НЕ восстановимы задним числом (WB-лимит):** где Воронка есть, мы = TS
  «в рубль» (22-24.05: заказы 314 = TS 314, сумма 1 787 788 = TS 1 787 788). Где
  Воронки нет (18-21.05) — fallback wb_orders (Statistics API, БЕЗ рассрочки/
  отменённых): 18-20 у нас 181 vs TS 441; полный 18-24 = 358 vs TS 871. Причина:
  WB v3 sales-funnel = rolling-7, истории не отдаёт (400); v2 nm-report (умел
  историю) WB отключил в 2025; наша funnel-фича включена 28.05 → раньше ~22.05
  данных нет и не будет. Решение: Воронка КОПИТСЯ ежедневно — будущие периоды
  сойдутся сами. `_funnel_covers_period` пофикшен (требует покрытие каждого дня,
  иначе fallback) — убирает частичный недосчёт (давал 314 вместо 358 на полном
  периоде, смешивая funnel 22-24 + пропуск 18-21).

## Финансовый лаг свежих недель (2026-06-06) — НЕ баг
Сверка 29.05-04.06 (только что закрытая неделя): наш financial-mode realisation
472 742 (только дни 29-31.05, published), TS 982 712 (все 7 дней). Заказы 582=582
(Воронка live). Причина: WB ещё НЕ опубликовал фин-отчёт (reportDetailByPeriod) за
01-04.06 — триггернул report_detail sync, 01-04.06 всё ещё 0 (WB не отдаёт). TS
заполняет неопубликованный хвост ОПЕРАЦИОННОЙ оценкой (sale_dt/preliminary), мы в
financial-mode строго ждём фин-отчёт. На ПОЛНОСТЬЮ опубликованных неделях (18-24.05)
оба сходятся «в рубль».
**СДЕЛАНО (v0.64.11/12, «живой хвост»):** /summary-report заполняет неопубликованные
дни операционной оценкой по wb_sales (подтверждённые выкупы): realisation=Σ
price_with_disc, sales=Σ finished_price, to_transfer=Σ for_pay. Отдаёт
`published_through`/`estimated_from`, на фронте — бейдж «оценка по выкупам».
Закрытые недели НЕ трогаются (estimated_from=None → байт-в-байт, 18-24 не сломана).
Результат 29.05-04.06: COGS 131 144 = TS точно, заказы 582 = TS, realisation
1 072 407 vs TS 982 712 (+9%), sales +9%. Остаток +9% — проприетарный forecast TS
для ещё-не-подтверждённых выкупов (TS realisation ниже нашего price_with_disc при
том же кол-ве; между finished_price и price_with_disc). Принято как ОЦЕНКА (±~10%,
сходится к точному факту при публикации WB). Гнаться за их форкастом смысла нет.

## Постраничный аудит остальных страниц (2026-06-06, vs живой TS)
- **Сводный по бизнесу** (`/business-summary`) — ✅ = TS точно (realisation/sales/
  toTransfer/sold 394).
- **Операции / ДДС(как TS) / План-факт / Дашборд** — ✅ работают. ДДС и План-факт —
  ручные инструменты, пусты пока пользователь не ввёл данные (как TS).
- **Склады** (`/stocks/by-warehouse`) — РАЗОБРАНО (2026-06-06): снапшот был старый
  (05-20, ~17 дней). Код синка ИСПРАВЕН — ручной триггер `/api/settings/sync/trigger
  {entity:stocks}` мгновенно дал свежий снапшот (06-06, 1840 строк). После refresh
  склады = TS: наш qty 4 434 vs TS stockBalance 4 427 (Δ7, 0.16%), in_way_from
  253 vs 256. Корень застоя: WB троттлит категорию `statistics` (/supplier/stocks)
  для этого продавца → beat 18:30 МСК часто не может обновить (DEV-055). Ⓘ
  checkpoint иногда показывает ok/1837 без свежего снапшота (расхождение
  checkpoint↔данные — отдельный мелкий баг, требует psql для подтверждения).
  Практика: on-demand refresh когда WB отвечает. Fetch — прямой WB API (НЕ
  extension-proxy).
- **⚠️ РЕКЛАМА ВОЛАТИЛЬНА (важно, ломает parity прибыли/ДРР):** WB fullstats для
  этого throttled-продавца отдаёт НЕСТАБИЛЬНЫЕ данные. ad_stats sync сейчас
  `status=skipped` («no fullstats data returned»). За ночь 05-06→06-06 реклама за
  18-24 пропала: было 19 705 → стало 0 (в WbAdStatsDaily осталось только 05-07..15
  = 5 446). Логика DEV-056 (delete только fetched_dates) КОРРЕКТНА — данные следуют
  за WB: WB начал отдавать 18-24 с 0 spend, мы перезаписали. TS этого не теряет, т.к.
  ФИКСИРУЕТ рекламу при первом захвате. ❗Решение требует продуктового выбора:
  «замораживать» ad spend (не перезатирать ненулевое нулём) — расходится с live WB,
  но даёт стабильность как TS. Эндпоинты (/ad-campaigns/analytics, /ads/heatmap)
  корректны; heatmap юзает алиасы from/to (не start_date/end_date).

## Sync-надёжность (WB rate-limit)
- WB агрессивно режет этого продавца (429 «Limited by global limiter»). Из-за этого:
  - Реклама — был wipe-баг (delete весь диапазон → потеря при 429). **Починено.**
  - Склады — синк защищён (early-return при пустом fetch, без delete), НЕ теряет
    данные, но застаивается пока WB throttle. Самовосстанавливается (manual refresh
    сработал → 05.06). Код-фикс не нужен; нужен мониторинг свежести.
  - Заказы — funnel неполнота больше НЕ роняет цифру (fallback на wb_orders).

## TODO
- [x] ДДС — TS-копия `/cashflow-calendar` (дневной календарь из ManualOperation), v0.63.0.
- [x] План-факт — TS-копия `/metric-plan-fact` (планы по метрикам, факт из dashboard), v0.64.0, миграция 0072.
- [x] OPEX из TS заведён (12 000 Аренда + 75 000 ФОТ = 87 000, /opex id 6,7).
- [x] Заказы — fallback funnel→wb_orders при неполном покрытии (v0.64.7/8, DEV-058).
- [x] Прочие удержания — штрафы вынесены отдельной плиткой (v0.64.7).
- [x] Прибыль — profit −= прочие удержания (СВЕРЕНО с живым TS: формула точна).
- [x] Прочие удержания — «WB Продвижение» исключена (bonus_type ILIKE продвиж),
      → 22 990 = TS точно, profit 481 093 ≈ TS 482 206 (v0.64.9).
- [ ] ❗Заказы старых недель (до ~22.05) — невосстановимы (WB не отдаёт историю
      Воронки). Только копить вперёд. Объяснить пользователю, не «чинить».
- [ ] Налоговая база (опц.) — выяснить точную формулу TS `taxBase` (Δ0.26%, оставили нашу).
- [ ] Реклама — ad Δ806 (наш 19 705 vs TS 18 900) — date-attribution / источник.

ДДС/План-факт у TS — **ручные** инструменты (не WB-аналитика): ДДС из ручных кэш-
операций, План-факт из созданных пользователем планов. «Копейка в копейку» = тот же
инструмент + те же ручные данные. Структуры скопированы 1:1; факт План-факта
считается нашим dashboard-движком (final/financial)."
