# QA + Seller feedback — раунд 14 (2026-05-26)

Reviewer: Claude Opus 4.7 (QA + UX-Validator --as seller). Метод: чтение кода
без запуска прода. Версия: v0.38.0.

## Скан фич (10)

- **Dashboard composite `<StateOfBusinessCard />`** (HYP-001, v0.37.0) — Working. 4 таба (Прибыль / Сверка с WB / Сегодня vs Вчера / Алерты), default «Прибыль». Lazy-loading реализован честно (см. ниже). A/B toggle composite|legacy в верхнем-правом углу `/dashboard` persist в localStorage. Default = composite. ManagerPlanProgressCard и OwnerCockpitView сохранены вне composite — оправдано.
- **ReconciliationHero polish** (TASK-LEAD-096, v0.38.0) — Working. Split на 2 мини-карточки в `md:grid-cols-2` (Сверка с WB + Доля выплаты), каждая со своим deep-link `#period=`. Hide-when-no-data сохранён. Старая 3-cell ячейка «Порог Δ» убрана из widget'а (но осталась в composite tab).
- **WeekProfit «vs 4-week avg» tab** (TASK-LEAD-097, v0.38.0) — Working. Toggle «WoW / vs 4-нед среднее» в шапке виджета. `avg4wQ` правильно `enabled: compareMode === "avg4w"` (lazy). Disabled tab + tooltip-объяснение при отсутствии данных, fallback на WoW.
- **TransitCalculator wizard-mode** (TASK-LEAD-093, v0.38.0) — Working. Toggle «Подробная форма / Упрощённая (wizard)» в правом-верхнем углу страницы. В wizard-режиме скрыты 4 секции (двухступенчатая шкала, override прямой поставки, multi-warehouse compare, доп-блок). Persist в `localStorage["transit.simple-mode.v1"]`. Default = НЕ wizard (full form) — `loadSimpleMode() → false`.
- **TransitCalculator stale-banner** (TASK-LEAD-094, v0.38.0) — Working с edge-кейсом (см. QA #1 ниже). Порог = 30 дней. При `synced_at>30d` рендерится оранжевая плашка под зелёной «Тариф из ЛК WB обновлён …». CTA-ссылка на ЛК WB.
- **Кастомный `<Dialog>`** (TASK-LEAD-090, v0.38.0) — Working. Replace native confirm() в: TransitCalculator (conflict при auto-fill ЛК WB → manual edit) + WeeklyReport TG-share (4 state: share-self, share-directors, pdf-fallback-no-tg, pdf-fallback-no-directors). ESC + backdrop-click работают, focus на confirm-кнопке.
- **BUG-DEV-015 (transit upload Pydantic + 0060 source_url)** — Fixed. Pydantic `Field(default=None, max_length=512)`, `_SOURCE_URL_WHITELIST_RE` whitelist для `*.wildberries.ru`. Extension передаёт `sourceUrl` через postMessage и upload. Миграция 0060 nullable column, upsert через `stmt.excluded.source_url`.
- **BUG-DEV-016 (manualEdited флаг + asynchronous Dialog)** — Fixed. `manuallyEdited: {rate_small, rate_large, volume_threshold_l}` отслеживает per-field manual edits. Auto-fill не overwrite-ит manual без подтверждения. Conflict UI через `<Dialog>` (заменил `window.confirm`). Сброс флагов при смене пары (хаб+склад).
- **BUG-UI-007 (MetricBreakdownPopup middle/meta/ctrl/shift)** — Fixed. На `<tr>` проверка `e.button === 1 || e.metaKey || e.ctrlKey || e.shiftKey` → не дёргает `onClose()` (новая вкладка через `<Link>`). Обычный левый клик → navigate + close.
- **TASK-LEAD-021 smoke-checker** (v0.36.0) — Scoped out (вне UI-фокуса этого review). Bash-скрипт `scripts/redistribution-smoke.sh` — не критично для seller UX. **HYP-006 weeks_window** (v0.36.0) — Working, selectbox 1/2/4/8/12 нед в TransitCalculator, persist в `localStorage["transit.weeks_window.v1"]`. **DocPage расширен** (TASK-LEAD-095) — Working: backend whitelist 5 slugs, docker-compose 5 mounts, frontend TITLES для всех 5. 3 новых .md в корне репо.

## QA замечания (баги / риски)

| Severity | Фича | Что | Где |
|---|---|---|---|
| **P1 (новый)** | StateOfBusinessCard / ReconciliationHero | `payoutCls` в обеих компонентах меряет «норму» как `payoutShare >= 95 && <= 100`. **Но** для seller'а с положительной разовой компенсацией от WB payout может быть >100% (например 102%). Текущая логика отнесёт это в `text-warn` (вне нормы) → seller паникует «что-то не так». Threshold должен быть `>= 95 && <= 105` (как в StateOfBusinessCard composite tab) — между двумя компонентами рассинхрон. | `ReconciliationHeroWidget.tsx:85` (`<=100`) vs `StateOfBusinessCard.tsx:248-249` (`<=105`). Один из двух. |
| **P1** | StateOfBusinessCard | `ProfitTab.curQ` использует `api.dashboard({start, end}, "final", reportingMode)` — для manager'а в `reportingMode==="financial"` (если он успел переключить до того как TASK-LEAD-058 скрыл toggle) или для new tenant без final-данных за прошлую неделю получит `curProfit = null` и покажет «Нет финальных данных…». **Но** `ReconciliationTab` грузится только при клике — нет fallback'а на «открыть другой tab если default пуст». Юзер открывает дашборд первый раз → видит «Нет данных», не понимает что делать. | `StateOfBusinessCard.tsx:145-152` |
| **P2** | StateOfBusinessCard | `ProfitTab` всегда грузит `tvyQ` (today-vs-yesterday) даже когда юзер открыл только Прибыль и не собирается смотреть Today tab. Eager-load на одном request'е (а не на 4). Не блокер — TanStack queryClient кеширует, при переключении на Today tab будет cache hit. Но в pure-lazy подходе этот запрос можно отложить (`enabled: false` + prefetch on hover). | `StateOfBusinessCard.tsx:133-136` |
| **P2** | StateOfBusinessCard | `ReconciliationTab` запрашивает `api.pnlReconciliation(4, 1.0)` (4 недели) и использует только `[0]`. Тот же запрос есть в `ReconciliationHeroWidget` (legacy), `queryKey` тот же → кеш переиспользуется. **Но** Composite tab не отрисуется до click — это первый запрос будет на click. Можно `weeks=1`. Не критично. | `StateOfBusinessCard.tsx:219` (`pnlReconciliation(4)`) |
| **P2** | StateOfBusinessCard | `AlertsTab.visible` фильтрует `acknowledged_at == null`. **Но** badge с counter показывает `activeAlerts.length` (тот же фильтр). Если после ack badge становится 0 → пользователь видит tab «Алерты» без badge + при клике «Нет активных алертов». OK. Но **нет UI для «Прочитанных»** — в `AlertsBar` legacy был expander «Прочитанные» с метаданными ack-er'а + кнопкой undo. В composite tab — потерян. Юзер не может un-ack, не может посмотреть кто из команды снял алерт. | `StateOfBusinessCard.tsx:404-451` |
| **P2** | TransitCalculator stale-banner | Banner рендерится только когда `transitFromBackend` существует (т.е. тариф был залит когда-то). Если `synced_at` строка из DB парсится в NaN — `ageDays = 0` → no banner. Случай `synced_at = null` (раньше не было upload'а) обрабатывается через ветку `transitFromBackend ? … : (manual-fallback)` — там banner stale не релевантен. Но если `synced_at` пришёл в неожиданном формате (без `Z`, naive datetime), `new Date(…).getTime()` может вернуть NaN → silent no-banner. Защиты от parsing-failure нет. | `TransitCalculator.tsx:836-842` |
| **P2** | TransitCalculator stale-banner | Порог `>30 дней` хардкод. WB по факту меняет тарифы ~раз в 1-3 месяца, но extension должно тригериться чаще (юзер открыл ЛК → расширение засинкало). Реалистичный сценарий: юзер не заходил в ЛК WB 2 месяца, тариф автоматически не обновился → banner оранжевый, юзер думает «надо обновить» → идёт в ЛК → ничего не меняет (WB не повышал) → возвращается, banner всё ещё оранжевый, потому что новый `synced_at` всё равно в прошлом, пока extension не успел снова перехватить fetch. То есть **banner не сбрасывается просто открытием ЛК**, нужно специально дойти до страницы «Транзитные направления». UI-механика хрупкая. | `TransitCalculator.tsx:842` (`isStale = ageDays > 30`) |
| **P2** | Dialog компонент | `confirmClass` идентичен для `default` и `danger` (`"btn-primary text-xs"` обоих ветках). Различие только в `confirmStyle = { background: "var(--danger)", borderColor: "var(--danger)" }`. **Но** при variant=`danger` inline-style для background применяется поверх класса, но `text-white` (или цвет текста primary) не applied — кнопка получит красный фон, но текст будет того же цвета что у primary (вероятно белый ОК, но на danger-bg не проверено). На light theme может быть проблема с контрастом. | `Dialog.tsx:86-93` |
| **P2** | A/B toggle composite/legacy | Кнопки `composite` / `legacy` в DOM в правом-верхнем углу `/dashboard`, текст «🆕 Compact» / «Legacy» (en/ru mix). При случайном клике юзер меняет режим без подтверждения — legacy грузит 6 разных API-запросов. **State-leak**: при переключении composite→legacy и обратно — `StateOfBusinessCard` ре-mount'ится (active tab сбрасывается на «Прибыль», queries делают refetch). Но при composite→legacy и закрытии вкладки → composite tab state потерян. Не критично, но если юзер часто гоняет toggle — потеря focus. | `Dashboard.tsx:223-241` |
| **P3** | ReconciliationHeroWidget vs Composite tab | Между legacy и composite разная **семантика** ячеек: в composite — Сверка с WB / Доля выплаты / Порог Δ (3 в grid-cols-3), в legacy — 2 separate cards (без «Порог Δ»). Юзер переключает A/B toggle → теряет «Порог Δ» в legacy mode. Не баг, но disonance. | `ReconciliationHeroWidget.tsx:281-315` (composite внутри SOB) vs `StateOfBusinessCard.tsx:281-315` |
| **P3** | WeekProfit avg4w | `priorThreeWeeks(week)` начинает с `sun-1` и идёт назад на 21 день включительно. То есть это **3 полные недели**, не 4. Метка таба «vs 4-нед среднее» технически неправильна (4 включая текущую, или 3 предыдущие). Tooltip пишет «средняя за 3 полные недели перед текущей». Расхождение метка vs tooltip. | `WeekProfitHero.tsx:70-76` (3 недели) + `:284` (label «vs 4-нед среднее») |
| **P3** | WeekProfit avg4w | `avgWeeklyProfit = avg3wProfitTotal / 3` делит на константу 3 без проверки сколько недель реально вошло (если WB-final за одну из недель пустой). Если за 1 из 3 недель данных нет → average занижен. Альтернатива: запросить per-week и считать avg только по non-null. | `WeekProfitHero.tsx:176-178` |
| **P3** | TransitCalculator wizard | Toggle «Подробная форма / Упрощённая (wizard)» в правом-верхнем углу страницы. Default = НЕ wizard (full form). Для нового юзера который зашёл первый раз — он увидит full form (13 полей) сразу. Wizard надо включать руками. С UX-точки зрения для onboarding'а хотелось бы default `wizard=true` и тогда юзер вынужден переключиться на advanced когда нужно. | `TransitCalculator.tsx:452-457` (`loadSimpleMode() → false`) |
| **P3** | TransitCalculator wizard | В wizard-режиме скрыт `compare` блок (multi-warehouse). **Но** «довоз до хаба» (delivery_to_hub_*) НЕ в wizard-список скрытых — они остаются видимыми. Это два числовых поля, которые seller часто оставит 0 → они и в wizard'е продолжают занимать место. Можно дополнительно auto-collapse. | `TransitCalculator.tsx:1153,1162,1280,1461,1584` (`!simpleMode`) — delivery_to_hub без `!simpleMode` |
| **P3** | Dialog компонент | `useEffect` с ESC handler перевешивается при каждом изменении `onCancel` (closure). Если в parent передан inline `() => setX(null)` (как в TransitCalculator `:1854`) — каждый rerender создаст новый handler. Не функциональный баг, но overhead. Worth a `useCallback` в caller. | `Dialog.tsx:63-80` + `TransitCalculator.tsx:1854` |
| **P3** | Dialog компонент | Backdrop click закрывает диалог даже когда юзер промахнулся мимо confirm-кнопки. Для destructive action (variant=`danger`) — backdrop-close = тихий cancel — это ОК. Но для conflict-dialog'а (Transit), где юзер уже принял ментально «применю значения», случайный mouse-out клик закроет диалог и юзер уйдёт неуверенным «я подтвердил или нет?». В TransitCalculator используется default variant — не блокер. | `Dialog.tsx:97-101` |
| **P3** | DocPage | 3 новых методички (TRANSIT / SUPPLY / RECONCILIATION) живут в корне репо, mount'ятся docker-compose, читаются backend'ом. **Но** `/docs/`-список (Helper guide `Docs.tsx`) их не знает — юзер не найдёт что доступно. DocPage показывается только когда юзер кликнет ссылку из конкретной страницы (если такая ссылка есть). На `TransitCalculator.tsx:1879-1881` единственная ссылка — `<code>agents/references/research-transit-shipments-2026-05-22.md</code>` (внутренний путь, не URL). Линк на `/docs/transit-calculator` в коде страницы отсутствует. | `TransitCalculator.tsx:1879-1881` (нет UI-ссылки) |
| **P3** | DocPage | `_SOURCE_URL_WHITELIST_RE` в transit_tariffs (`source_url`) проверяет только wildberries.ru. Если WB меняет домен (cdn.wildberries.ru → seller-portal.wb.ru) — extension зальёт но backend отклонит на 400 без явной ошибки. Audit-trail сломается. | `backend/app/api/transit_tariffs.py:186` |

## UX-as-seller замечания

«Утро, открываю дашборд. Что вижу первым?»

| Степень боли | Фича | Что | Что предлагаю |
|---|---|---|---|
| Critical | StateOfBusinessCard default tab | Default tab = «Прибыль» — net_profit за прошлую закрытую неделю. **Это с лагом 14 дней.** Для собственника который открывает дашборд утром понедельника — единственная цифра, которую он видит первой, это **двухнедельной давности**. «Сегодня vs Вчера» (актуальные данные) спрятан на 3-м табе. Composite свернул информацию, но не правильно расставил приоритет. | Поменять default на «Сегодня vs Вчера» для всех ролей кроме bookkeeper'а (он Dashboard и так не видит). Прибыль убрать на 2-й tab. Альтернативно — оставить «Прибыль» для собственника, но «Сегодня vs Вчера» для head_of_sales и manager. |
| Critical | StateOfBusinessCard «Прибыль» tab | Для seller'а который только что подключил кабинет (новый юзер, демо-период) — `curProfit == null` (ещё не было final-отчётов от WB). Tab показывает «Нет финальных данных за прошлую неделю». **Но default tab по-прежнему «Прибыль»**. Юзер видит «Нет данных» и думает «приложение не работает». Нужен auto-redirect на первый tab с данными (например, «Сегодня vs Вчера» если preliminary есть). | Если default tab пустой — auto-switch на первый non-empty (Today → Alerts → Reconciliation → Profit fallback). Альтернативно — empty-state с CTA «открой Сегодня vs Вчера». |
| Moderate | A/B toggle composite/legacy | Текст «🆕 Compact» / «Legacy» — мешанина EN/RU. Для русскоязычного юзера «Compact» = непонятно, «Legacy» = старый что? Без контекста. Tooltip объясняет но если не наведёшь — гадаешь. | «🆕 Сводка / Полный вид» или «🆕 Краткий / Подробный». «Legacy» — заменить, technical jargon. |
| Moderate | ReconciliationHero split | 2 мини-карточки бок-о-бок на `md:` (≥768px) — ОК. На laptop 1280px они получают по 600px ширины — выглядят полупустыми, каждая занимает ~25vh. Сократили 30vh→25vh за счёт 2 cards вместо 1 трёхколонной — экономия минимальная. Если плотность важна — `lg:grid-cols-2 md:grid-cols-1` чтобы на узких экранах stack. **Но** объективно — 2 раздельные карточки лучше чем смешение «сходимость» + «доля выплаты» в одной. Это шаг в правильную сторону. | Использовать только в composite-mode toggle. Legacy mode пусть остаётся 1 трёхколонная (для тех кто привык). Текущая реализация — split применяется к legacy-mode тоже. |
| Moderate | WeekProfit «vs 4w avg» | Метка таба «vs 4-нед среднее». Tooltip: «средняя за 3 полные недели перед текущей». **4 ≠ 3**. Собственник кликает и потом считает в уме: текущая + 3 предыдущих = 4 → ОК, «4-week window with current excluded». Но в названии непонятно. Лучше явно «vs ср. за 3 пред. недели» или «vs 4w avg (без текущей)». | Переименовать в «vs ср. за 3 пред. нед.» — точно отражает что считается. |
| Moderate | WeekProfit «vs 4w avg» | Tab disabled когда `!avg4wAvailable` — но disabled state не объясняет ПОЧЕМУ disabled («Нет данных за 3 предыдущие полные недели» — в tooltip). Лучше badge типа «(скоро)» прямо на кнопке. | Visual hint типа `(нет данных)` курсивом рядом с label. |
| Moderate | Dialog в TransitCalculator (conflict) | Conflict-dialog появляется когда auto-fill из ЛК WB хочет overwrite manual-edit. Текст: «📊 Тариф обновлён в ЛК WB. Применить новые значения? Отмена — оставить ваши значения. OK — применить новые». **Confirm-кнопка «Применить»** — single CTA. Юзер обычно не помнит «какие были мои значения», даже если diff показан. Risk: случайно кликнет «Применить», забудет что было. | Сделать «Отмена» (= оставить мои) primary (default-focus), а «Применить» secondary. Текущая реализация: focus на confirm = `Применить`. Это destructive default. |
| Moderate | TransitCalculator wizard | Wizard mode скрывает 4 секции, но в верху страницы остаётся **2 banner'а** (Тариф из ЛК WB + stale), 1 toggle (wizard / detailed), 1 weeks_window selectbox, 1 page header, 1 SKU picker, основная форма. Всё ещё ~6-7 sections видны до wizard'a. Wizard упростил, но не radically. Реалистичный baseline для нового юзера — 4-5 fields. | Дополнительно скрыть «довоз до хаба» (delivery_to_hub_*) если значение = 0. Plus «Тариф из ЛК WB» banner — оставить только когда юзер кликнул «explain why this banner». |
| Moderate | DocPage 3 новых методички | Файлы существуют, mount'ятся. Размер TRANSIT 157 / SUPPLY 97 / RECONCILIATION 156 строк — для seller'а тяжеловато, особенно TRANSIT (с code snippets `total_volume = units × liters_per_unit`). RECONCILIATION содержит 10 секций — это уже не «методичка», а спецификация. **Юзер с дашборда не попадёт на эти доки** — только через прямой URL `/docs/reconciliation`. На странице `/transit-calculator` нет ссылки на `/docs/transit-calculator`. На `/pnl-reconciliation` — тоже не проверял, но скорее всего нет. | Добавить «📖 методика» link-кнопку в page-header каждого калькулятора и `/pnl-reconciliation`. Без link'ов — доки мертвы. |
| Minor | StateOfBusinessCard «Алерты» tab badge | Badge показывает количество всех unack alerts, но цвет = severity самого опасного (danger > warning > info). Если 5 info-алертов + 1 danger → badge будет красный «6», info-алерты тонут в красном. Юзер кликает, ожидая «6 опасных», видит 1 danger + 5 info, чувствует обман. | Badge может быть split типа `1🔴/5🔵` или просто `6` с цветом info (наименее тревожный) — заставляет открыть и посмотреть. |
| Minor | StateOfBusinessCard «Алерты» tab | Нет «Прочитанные» expander как в legacy AlertsBar. Юзер не может проверить кто из команды снял алерт, не может un-ack. Регрессия по сравнению с legacy. | Добавить collapsible «X прочитанных» с метаданными + undo. Или хотя бы FAQ tooltip «прочитанные смотри в /audit-log». |
| Minor | TransitCalculator stale-banner | Текст: «WB регулярно меняет тарифы (последнее повышение +20% с 2026-04-01)». Сегодня 2026-05-26, эта дата уже **в прошлом > 30 дней** для большинства тарифов. Banner будет показывать «+20% с 2026-04-01» как актуальное напоминание даже когда WB следующее повышение сделает в сентябре. Хардкод даты повышения. | Брать из конфига/секунд-config'а или вычислять «последнее изменение в нашей БД». Если в БД нет — generic «WB периодически повышает». |
| Minor | Dialog ESC и backdrop close | ESC и backdrop-click оба = onCancel. Для conflict-dialog (Transit) — backdrop-click может произойти случайно при попытке кликнуть «Применить» (промахнулся). Дефолт-cancel = «оставить мои значения» — это безопасно. OK. | Не критично. |
| Minor | DocPage в TransitCalculator | Footer page'а: `Research-методичка: agents/references/research-transit-shipments-2026-05-22.md`. Это internal-path, не URL. Seller не может его открыть. | Заменить на link `/docs/transit-calculator` (есть в whitelist!) или вообще убрать research-ссылку. |
| Minor | WeekProfitHero scope-label для manager'а | TASK-LEAD-083 закрыл BUG-UI-006 — для manager'а header теперь «За неделю X-Y по твоим брендам». Хорошо. **Но** теперь header для manager'а длиннее (28 chars vs 22 для собственника). На узких экранах может wrap'нуться в 2 строки. Не критично, но visual layout shift между ролями. | Не блокер. Можно `truncate` на manager-варианте. |
| Minor | WeekProfit Dialog conflict | Banner-цвета: зелёный (ОК) vs жёлтый (нет тарифа) vs оранжевый (stale) — 3 уровня. Юзер должен помнить семантику цветов. **Зелёный + оранжевый одновременно** (есть тариф но stale) — два banner'а stack'аются → 2 строки. Когда оба фоновых style inline через `rgba(…)` — visual noise. | Объединить в один banner с conditional иконкой 📊✅ / 📊⚠ + текст. |

## Coverage gaps

### Round 13 незакрытые

- **HYP-001 default tab orientation** — раунд 13 предлагал «composite больше чем nice-to-have», это сделано (HYP-001 v0.37.0). Но **выбор default'а** (Прибыль) — не обсуждался, оказался непродуманным для new-tenant сценария и для оперативного workflow собственника утром. Идёт в round-14 (см. Critical UX выше).
- **HYP-003 merge** — реализовано (`HYP-003 v0.37.0`) как soft merge с `/localization` и калькуляторами. Не валидировал отдельно (вне фокуса этого review).
- **WeekProfitHero RBAC для manager** — закрыто TASK-LEAD-083 через `headerLabel` (manager видит «по твоим брендам»). Виджет не скрыт, как в одном из вариантов round-12, но scope сделан явным. OK.
- **WeekProfit vs 4w avg** — реализовано TASK-LEAD-097. Имеется issue с метрикой 3 vs 4 (см. P3 выше).
- **DocPage расширить** — реализовано TASK-LEAD-095. Имеется issue: нет UI-ссылок с страниц калькуляторов на доки (см. P3 + Moderate UX выше).
- **TransitCalculator wizard** — реализовано TASK-LEAD-093. Скрывает 4 секции из 6+. Default = full form (можно поспорить).
- **Stale-tariff banner** — реализовано TASK-LEAD-094. Hardcoded 30d threshold + жёсткий «+20% с 2026-04-01» текст (см. Minor UX).

### Регрессии от реализованных фич

1. **AlertsBar в composite mode — потерян expander «Прочитанные»** (legacy `AlertsBar.tsx` имел collapsible с ack-метаданными + undo, в новом `AlertsTab` нет). Регрессия для команды, которая хочет видеть «Маша уже сняла этот алерт 2 часа назад» (видимость workflow).
2. **ReconciliationHero «Порог Δ» colmn потерян в legacy** — composite tab показывает 3 ячейки (Δ выручки / Доля выплаты / Порог Δ), а legacy 2-card вариант показывает 2 ячейки (Δ выручки / Доля выплаты), пороговая метрика спрятана в footer-текст «Порог ≤ 1% (≈ X тыс ₽)». При A/B switch теряем визуальное представление о пороге.
3. **WeekProfit avg4w в новом кабинете** — disabled tab без явной причины («Нет данных» в tooltip, но кнопка disabled и серая → seller думает «приложение глючит»).
4. **Dialog компонент** — стиль `confirmClass` для variant=`danger` идентичен default-у (только background меняется через inline style). Если в будущем добавят dark theme — может сломаться.

### `USER_GUIDE.md` gaps

- **Composite State of Business** — описан (`#дашборд--composite-state-of-business`, секция 7). OK.
- **Wizard-mode для TransitCalculator** — не описан в USER_GUIDE.md (ищу `simple-mode` / `wizard` → 0 matches кроме TOC). Юзер не узнает о существовании режима из guide.
- **WeekProfit «vs 4w avg»** — не описан.
- **Dialog vs native confirm** — это internal change, не для guide. OK.
- **Stale-banner для transit** — не описан.
- **3 новых методички** (TRANSIT / SUPPLY / RECONCILIATION) — нет cross-link'ов из USER_GUIDE.md на `/docs/{slug}`. Юзер должен догадаться о URL'ах.

## Идеи для синтеза (без типизации — Lead решит)

- **CRITICAL UX: Default tab StateOfBusinessCard** должен быть smart-defaulted (Today vs Yesterday если final пусто, или по роли). Сейчас default = Прибыль и для new tenant'а = «Нет данных».
- **CRITICAL UX: Auto-redirect к first non-empty tab** при `curProfit == null` — иначе seller видит «Нет данных» как первое впечатление.
- **P1 рассинхрон threshold'а Доля выплаты** между ReconciliationHero (`<=100`) и StateOfBusinessCard composite (`<=105`) — должен быть одинаковый.
- **AlertsBar «Прочитанные»** регрессия в composite — добавить expander в AlertsTab.
- **WeekProfit avg4w label** «vs 4-нед среднее» нелогично (по факту 3 предыдущие). Переименовать.
- **WeekProfit avg4w handling** при пропуске недели (делит на 3 без проверки) — некорректный average.
- **Dialog focus** — для conflict-dialog (Transit) лучше focus на «Отмена», а не на «Применить» (destructive-default-cancel).
- **TransitCalculator wizard default** — для нового юзера лучше default `wizard=true`, чтобы onboarding с 4 полей, а не 13.
- **DocPage UI-ссылки** — добавить «📖 методика» link в page-header каждого калькулятора и `/pnl-reconciliation`. Без ссылок 3 новых .md (TRANSIT/SUPPLY/RECONCILIATION) недоступны юзерам.
- **TransitCalculator footer research-link** — заменить internal-path на `/docs/transit-calculator`.
- **Stale-banner text «+20% с 2026-04-01»** — хардкод в коде. Через 3 месяца устареет. Сделать конфиг или вычислять.
- **`source_url` whitelist regex** — расширить на потенциальные WB-домены (sub.wb.ru, cdn.wildberries.ru), либо снять whitelist (audit-only поле).
- **USER_GUIDE.md gaps** — добавить разделы про wizard-mode, vs 4w avg, stale-banner, ссылки на 3 новых методички.

## Что чисто (нашёл, нечего сказать)

- **Lazy-loading в StateOfBusinessCard** — реализовано честно: каждый tab grows queries по active state, кроме `tvyQ` который грузится eagerly в ProfitTab (для footer-context «Сегодня / вчера revenue»). TanStack кеширует, при переключении на Today tab cache hit.
- **A/B toggle composite|legacy** persist в `localStorage["dashboard.hero-mode.v1"]`. State через `useState(loadHeroMode)`. При unmount/remount state восстанавливается. OK.
- **WeekProfit avg4wQ.enabled** — `enabled: compareMode === "avg4w"` — реально lazy, не грузится пока юзер не переключит таб.
- **WeekProfit avgWeeklyProfit fallback** — `compareMode === "avg4w" && !avg4wAvailable && !avg4wQ.isLoading → "wow"` (effective mode). Юзер не видит NaN, fallback корректный.
- **Dialog ESC + backdrop** — оба onCancel. Focus при mount работает (setTimeout(0) для после render). OK для accessibility.
- **TransitCalculator manuallyEdited** правильно сбрасывается `useEffect` на смену `params.hub / params.final_warehouse` — для новой пары auto-fill silently.
- **BUG-DEV-015 source_url** — миграция 0060 nullable + Pydantic Field max_length=512 + whitelist regex + upsert через `stmt.excluded.source_url`. Extension шлёт корректно через postMessage.
- **BUG-DEV-016 manualEdited** — `markManual` handler в onChange, conflict-list вместо bulk-confirm (per-field decision). Default cancel = безопасно.
- **BUG-UI-007 middle/meta/ctrl/shift** — корректный `e.button === 1 || e.metaKey || …` на `tr.onClick`, плюс `preventDefault()` для onAuxClick (middle = noop на tr). Новая вкладка открывается через `<Link>` без onClose.
- **ReconciliationHeroWidget hide-when-no-data** — `if (!latest) return null` — корректно. На новом кабинете не показывается.
- **DocPage whitelist** — slug → file mapping строгий, candidate-paths fallback на 3 локации (Docker /app, repo-root, parent), 404 при unknown slug — все edge'ы покрыты.
- **TASK-LEAD-091 (`/notifications` для head_of_sales)** — корректно в Layout whitelist (line 174). РОП теперь видит alert rules.
- **TASK-LEAD-095 docker-compose** — 5 read-only mounts для всех 5 doc-файлов. Backend candidate_paths найдёт их в /app.
- **WeekProfit compareValue null-safety** — `compareValue != null && …` везде в render, не падает на null. WoW и vsAvg оба могут быть null (нет prev / нет avg) — обрабатывается.
- **StateOfBusinessCard threshold ячейка** — корректное вычисление `thresholdThousands = round(gross * 0.01 / 1000)`, не отрицательное, не nan.
- **ReportingMode передаётся в API** — StateOfBusinessCard ProfitTab и WeekProfitHero оба передают `reportingMode` в `api.dashboard()`. Соответствует контракту backend'а (TASK-LEAD-080 fix).
