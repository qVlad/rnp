# QA + Seller feedback — раунд 13 (2026-05-25)

Reviewer: Claude Opus 4.7 (QA + UX-Validator --as seller).
Метод: чтение кода без запуска прода. Версия: v0.34.0.

## Скан фич (5)

- **PromoCalculator polish** (TASK-LEAD-050/067) — Working. 2-col layout после симуляции работает, sticky-form слева. `api.listProducts` правильно подменил прямой `fetch`. Doc-link на `/docs/promo-calculator` есть.
- **TransitCalculator** (TASK-LEAD-053/068/071/072/077/078) — Working с одним fragile-местом (extension auto-fetch без shape-validation telemetry). Multi-warehouse compare, SKU-aware picker, WoW тариф δ, override прямой поставки, довоз до хаба — все на месте.
- **WeekProfitHero** (TASK-LEAD-073, BUG-DEV-011) — Working. `reportingMode` теперь передаётся в `api.dashboard`. Header «За неделю DD-DD месяц (закрыта)» с реальными датами. **НО** RBAC-guard так и не добавлен — manager видит виджет (BUG-UI-006 был P3, не вошёл в раунд).
- **ReconciliationHeroWidget** (TASK-LEAD-063/069) — Working. Третья цифра «Доля выплаты» добавлена, threshold в тыс ₽ показан, deep-link `#period=...` рендерится, wizard explainer переведён в plain language.
- **MetricBreakdownPopup** (TASK-LEAD-066, BUG-DEV-010/012/013) — Working с одной новой регрессией: фикс BUG-DEV-013 корректный, но endpoint `/kpi-breakdown` не учитывает `reporting_mode` (см. ниже).

## QA замечания (баги / риски)

| Severity | Фича | Что | Где |
|---|---|---|---|
| **P1 (новый)** | 055 / breakdown | `GET /api/dashboard/kpi-breakdown` **не принимает** `reporting_mode` query param и `compute_kpi_breakdown` хардкодит `sale_dt_filter` (operational). Если seller переключил toggle в financial — главные KPI на дашборде идут по `rr_dt`, а breakdown popup по тем же KPI — по `sale_dt`. Σ breakdown ≠ KPI на границах rr_dt vs sale_dt (для возвратов разница 1-2 недели). Все остальные `/api/dashboard/*` endpoint'ы (kpis, today-vs-yesterday, etc.) `reporting_mode` принимают. | `backend/app/api/dashboard.py:122-142` + `services/kpi_breakdown.py:130` |
| P2 | 042 | `WeekProfitHero` всё ещё показан manager'у без role-guard (BUG-UI-006 числится в раунде 12 как P3, не вошёл в спринт). Manager видит виджет «За неделю …» с цифрой за свой brand-scope, при том что заголовок не упоминает scope — manager думает что видит компанию. Для bookkeeper'а Dashboard вообще скрыт в Layout — не проблема. | `frontend/src/pages/Dashboard.tsx:204` |
| P2 | 078 | Extension MAIN-world interceptor парсит произвольные WB-ответы по shape (`looksLikeTransitTariffs` требует ≥2 строки). Нет server-side schema validation на `POST /api/transit-tariffs/upload` кроме `rate ≥ 0` + min_length. Если WB изменит формат и shape-парсер случайно подхватит non-tariff data с числовыми полями — backend примет мусор без алерта. Нет аудита какой URL прислал данные (поле `url` есть в postMessage, но в backend `_row_to_dto` не сохраняется). | `extension/src/content/wb-transit-tariffs-interceptor-main.ts:146-159` + `backend/app/api/transit_tariffs.py:134-242` |
| P2 | 078 | Auto-fill в TransitCalculator (`useEffect` с зависимостью `transitFromBackend?.synced_at`) перезатирает manual-edits юзера при появлении/refresh тарифа. Условие `autoFilled === key` защищает только в рамках одной сессии. Если юзер открыл страницу, тариф ещё не синканулся, юзер вписал свой `rate_small=10`, extension через 5 минут синканул тариф 8 — поле молча обновится на 8. Юзер не увидит что его правка ушла, кроме banner «обновлён только что». | `frontend/src/pages/TransitCalculator.tsx:496-513` |
| P2 | 071 | `SkuPicker.selectedQ` для отрисовки чипа делает `api.listProducts({search: String(nm_id)})` и ищет точное совпадение по nm_id. Если nm_id длинная подстрока (например 12345 совпадает в vendor_code), полный поиск отдаст 30 рандомных результатов где может не быть искомого — чип останется в состоянии «…». Точечный endpoint `GET /api/products/{nm_id}` существует, но picker им не пользуется. | `frontend/src/pages/TransitCalculator.tsx:146-155` |
| P3 | 077/078 | `TransitTariffRow.synced_at: str` в Pydantic — backend возвращает ISO с `Z` или naive? `_row_to_dto` делает `r.synced_at.isoformat() if r.synced_at else None` — модель типизирует как `str`, при None возможен Pydantic-error при сериализации. Краш не вижу, но `Optional[str]` был бы честнее. | `backend/app/api/transit_tariffs.py:46-54` + `:77` |
| P3 | 073 | `lastClosedWeek()` (`WeekProfitHero.tsx:28-40`) откатывает к воскресенью локального TZ браузера. Для собственника в Москве — ОК. Для собственника в Калининграде/Владивостоке на границе недели — сдвиг на день относительно UTC `sale_dt` на бэке. Не блокер. | `WeekProfitHero.tsx:28-40` (без изменений с раунда 12) |
| P3 | 043 | `ReconciliationHeroWidget` всё ещё запрашивает `weeks=4`, использует только `[0]`. P3 ноут из раунда 12, не критичный. Можно поменять на `weeks=1` при следующем касании. | `ReconciliationHeroWidget.tsx:40` |
| P3 | 066 | `MetricBreakdownPopup` строки `<tr onClick>` имеют `onClick` для navigate + вложенный `<Link>`. Если юзер middle-click'нет по строке (в Chrome — открыть в новой вкладке), Link перехватит и откроет вкладку, но onClose всё равно вызовется — popup закроется на текущей. Не баг, но дискомфорт. | `MetricBreakdownPopup.tsx:179-209` |
| P3 | 067 | `PromoCalculator` — кнопка «Симулировать» disabled когда `nm_ids.length === 0` или `mut.isPending`, но **нет** disabled при невалидном discount (0 или 99). При discount=0 акция без скидки — расчёт работает, но бессмысленный. Не блокер, тривиальный edge. | `PromoCalculator.tsx:226` |
| P3 | 068 | Multi-warehouse compare использует `tariffsQ.data?.items` для lookup тарифа конечного склада, но **не** проверяет наличие транзитного тарифа для пары `(hub, new_warehouse)`. Если у юзера в backend есть тариф `Обухово → Казань`, но нет для `Обухово → Тула`, для Тулы будет посчитан транзит по `params.rate_small/large` (manual / прошлая авто-подстановка). Не вводит в заблуждение, но multi-warehouse compare предполагает чистое «куда довезти» — а тариф один на всех в текущей реализации. | `TransitCalculator.tsx:1382-1397` |
| P3 | doc | `DocPage` для unknown slug отображает «Документ «X» не найден». OK. Но whitelist в `doc_pages.py:_SLUG_TO_FILE` всего 2 slug'а (`promo-calculator`, `unit-plan`) — а ссылки на `/docs` (Helper guide) тоже работают через отдельный `Docs.tsx`. Если кто-то накатит `<Link to="/docs/transit-calculator">` — будет 404. Нет в коде, но риск роста. | `backend/app/api/doc_pages.py:24-27` |

## UX-as-seller замечания

«Утро, открываю дашборд, что вижу первым?»

| Степень боли | Фича | Что | Что предлагаю |
|---|---|---|---|
| Critical | 055 | Я кликаю на «Логистика WB» (например после financial-mode переключения для бухгалтерской сверки) → popup показывает 187к₽ за период. Главный KPI на странице показывает 203к₽. **Расходятся**. Я думаю что один из них врёт. На самом деле — разная семантика даты (sale_dt vs rr_dt). | Принять `reporting_mode` в `/kpi-breakdown` endpoint, передать в `compute_kpi_breakdown`, использовать `get_period_filter(...)` вместо `sale_dt_filter()`. До фикса — баннер в popup «🚧 popup всегда по дню выкупа, не зависит от toggle». |
| Moderate | 069 | Hero «Сверка с WB-кабинетом» теперь содержит 3 цифры (Δ% / Δ₽ / Доля выплаты) + footer с «Δ ≤ 1% — сходятся». Это **много**. На laptop экране карточка занимает ~30vh. Δ выручки и Доля выплаты — разные истории («сходимость» vs «сколько на счёт»), вместе они тяжёлые. | Раздельные карточки рядом или collapsible: основная — Δ + threshold; «Доля выплаты» — на /pnl-reconciliation deep tab. Альтернативно — убрать центральную колонку «Порог Δ» (это техническая подпись, не KPI). |
| Moderate | 073 | Header «За неделю 12-18 мая (закрыта)» лучше чем «Прибыль за прошлую закрытую неделю» — путаницы с «Прибыль вчера» нет. **НО** для собственника, который не помнит сегодняшнюю дату наизусть, «12-18 мая» в отрыве от текущей даты — абстракция. Хорошо бы «12-18 мая (4-5 дней назад завершилась)» или явно `Lag ~14 дней`. | Добавить relative подсказку «(закрыта 4 дня назад)» или сохранить «закрытая неделя» как явный термин (без удаления). |
| Moderate | 067 / 050 | 2-column layout: form слева 40%, results справа 60%. При узком окне laptop'а (1280px) form сжимается, и числовые поля (скидка/длительность) начинают наезжать друг на друга в `grid-cols-1 sm:grid-cols-2` (`hasResult ? sm:grid-cols-2 : md:grid-cols-2 lg:grid-cols-4`). При `hasResult` форма не gets полные 4 столбца — приходится скроллить. | На `lg:` exceeded — переключить layout обратно на 1-col stack чтобы форма не сжималась под результатами. Sticky отдельной задачей. |
| Moderate | 078 / 053 | TransitCalculator overload: 13 полей ввода (хаб, склад, кол-во, литры/шт, дни, прямой тариф ₽/л, two-tier small/large/порог за expander, override прямой поставки, довоз до хаба × 2 поля, multi-warehouse compare list × до 5 складов, SKU picker). Для одноразовой задачи (раз в месяц при поставке) — нормально, но первый раз заходишь и теряешься. | Wizard-like flow «Шаг 1: выбери товар (опционально). Шаг 2: партия. Шаг 3: тариф. Шаг 4: результат». Сейчас все sections видны сразу — visual overload. Альтернатива: collapse «довоз до хаба» / «override прямой» / «двухступенчатый тариф» если значения дефолтные. |
| Moderate | 078 | Banner «📊 Тариф из ЛК WB обновлён 5 мин назад» зелёный. Banner «🔄 Не нашли тариф для этой пары» жёлтый. Это **разные UX-состояния**, но между ними нет «есть тариф, но он устарел» (например 30+ дней). С 1 апреля 2026 WB поднял транзит на ~20% (упомянуто в footer-карточке) — если последний sync был до этого, юзер использует старый тариф. | Если `synced_at > 30 дней назад` — оранжевый «⚠ Тариф давно не обновлялся, проверь в ЛК». |
| Minor | 066 | Клик на ряд в `MetricBreakdownPopup` → переход на `/units?nm_id=X`. Хорошо, фильтр по nm_id применяется. **НО** на `/units` нет видимого индикатора что я попал сюда из breakdown'а (typu badge «Из breakdown: Логистика WB · период X»). Юзер забывает контекст откуда пришёл. | Опциональный URL-param `?from=breakdown&metric=logistics_wb` → баннер сверху `/units` «Вы пришли из breakdown'а: Логистика WB за X-Y». Не критично, polish. |
| Minor | 067 | PromoCalculator: при результате 10 SKU с большим количеством колонок (10 cols × ~80px = 800px+) — на 1280px laptop column сжимаются и horizontal-scroll включается. Названия колонок длинные («Минимум для окупаемости»). | Sticky-первая колонка (артикул), либо сокращение названий («Min boost для окупаемости» → «Min boost»). |
| Minor | 071 | TransitCalculator: SKU picker — opt-in, но после выбора SKU поля `units` и `liters_per_unit` подменяются. Если юзер уже ввёл свои значения, оно затрётся. Подсказка `suggestInfo` появляется после, но юзер уже **может не заметить** что его значения ушли. | Перед auto-fill — `confirm('Заменить introduced values?')` или показывать «Подставлены автоматически (откатить)». |
| Minor | 073 | WoW «+47.3%» зелёный — приятно. Но при низкой базе (пред. неделя = 50к, эта = 200к) собственник может не понять что это статистический шум. Раунд 12 предложил «vs 4-week avg» таб — не реализован в TASK-LEAD-073 (упоминается «опциональный таб» в описании, но коде нет). | Реализовать таб «WoW / vs 4w avg» — раунд 12 уже предлагал. |
| Minor | doc | `/docs/promo-calculator` — методичка читается, формулы корректные (сверены с `services/promo_calculator.py`). Длина 264 строки, секций 9 — для seller'а тяжеловато. Backend-internal термины («pure-function», «condecimal», «case((OP_SALE, REVENUE_FIELD)…»). | Если страница позиционируется для собственника — короче и без code-references; полную методичку оставить как development-docs. Если для всех — добавить TL;DR-табличку «когда вступать в акцию: 3 примера». Сейчас она идёт в самый конец секции 6. |

## Coverage gaps (что забыто из раунда 12 / нет в docs)

- **BUG-UI-006** (WeekProfitHero без role-guard для manager'а) — раунд 12 классифицировал как P3, в P2-спринт не вошёл, в P3-спринт тоже. Manager в production видит этот виджет с brand-scope цифрой без явного указания scope.
- **HYP-001** (composite «State of Business» вместо 4 hero-виджетов) — гипотеза не валидирована, добавили ещё больше виджетов на дашборд (Reconciliation 3-col, WeekProfitHero, TodayVsYesterdayStrip, WeeklyChangesFeed, AlertsBar, CustomMetricsCard, ManagerPlanProgressCard for manager). Top of dashboard теперь занимает 70-80% viewport на laptop. Гипотеза становится более актуальной.
- **HYP-003** (слияние `/localization` и `/transit-calculator` в `/redistribution`) — не валидирована. Транзит-калькулятор остался standalone (даже не expander), localization тоже отдельная страница.
- **`USER_GUIDE.md`** — содержит 19 упоминаний фич (промо/транзит/сверка/breakdown), но это короткие mentions. Полноценного раздела про каждый калькулятор + каждый Hero-виджет на дашборде нет. Для собственника, который видит экран первый раз — никакого guided tour.
- **DocPage** покрывает только 2 slug'а (`promo-calculator`, `unit-plan`). `TransitCalculator`, `SupplyCalculator`, `ReconciliationHeroWidget` — нет методички в `frontend/public/docs/`. На странице `/transit-calculator` ссылка на `agents/references/research-transit-shipments-2026-05-22.md` в footer (внутренний путь, не accessible через UI).
- Round-12 BUG-UI-006 не зашёл в текущий релиз. Round-12 «WoW vs 4-week avg» таб в WeekProfitHero — описан в TASK-LEAD-073 ноуте, не реализован.

## Идеи для синтеза (без типизации — Lead решит)

- **Critical bug (BUG-новый)**: `/kpi-breakdown` не уважает `reporting_mode` → Σ breakdown ≠ KPI при financial-mode. Фикс: добавить `reporting_mode` в endpoint + в `compute_kpi_breakdown`, использовать `get_period_filter()` вместо `sale_dt_filter()`. Стоит фикса с unit-test'ом (раунд 12 уже выявил отсутствие тестов для kpi_breakdown как риск).
- **WeekProfitHero RBAC**: либо скрыть от manager'а полностью (как BUG-UI-006 предлагал), либо явно подписать «по твоим брендам» в header при `user.role === 'manager'`. Сейчас тихое смешение терминов.
- **Transit auto-fetch UX**: indicate stale-tariff (sync >30d ago), confirm перед overwrite manual-edits, audit-log source-URL для extension uploads.
- **Multi-warehouse compare для transit**: текущая реализация считает разные склады с одним и тем же tariff (manual-fill). Если в backend есть тарифы для нескольких пар `(hub, dest)` — использовать их, не один shared tariff.
- **Top of Dashboard overload**: 6+ heros занимают viewport. Hypothesis HYP-001 «composite State of Business» становится прагматично необходимой, а не «может быть полезно».
- **WeekProfitHero «vs 4-week avg»** таб — упомянут в TASK-LEAD-073 описании, не реализован.
- **DocPage coverage**: добавить методички для `/transit-calculator` (TASK-LEAD-053 + 077/078 — пора пояснять, что такое транзит) и `/pnl-reconciliation` (wizard объяснил per-row, общая методика отдельно отсутствует).
- **TransitCalculator wizard-mode**: 13 полей overload для разовой задачи. Альтернативный вид «упрощённый» (только SKU + хаб + конечный, всё остальное auto/default).

## Что чисто (нашёл, нечего сказать)

- **BUG-DEV-013 (commission_wb знак)** — фикс корректный. Формула в `kpi_breakdown.commission_expr = REVENUE_FIELD - ppvz_for_pay - acquiring_fee` под `case(OP_SALE, ...) - case(OP_RETURN, ...)` точно совпадает с `metrics.py:_final_finance_aggregate` (line 344-362). Sum breakdown равен KPI commission_wb в operational mode.
- **BUG-DEV-010 (truncated «остальные»)** — фикс на месте: `total_items` + `truncated_sum` приходят с бэка, фронт отрисовывает «топ-N из M SKU; остальные суммарно X ₽».
- **BUG-DEV-011 (WeekProfitHero игнорировал reporting_mode)** — фикс корректный, `reportingMode` теперь часть `queryKey` и параметр `api.dashboard()`.
- **BUG-DEV-012 (kpi_breakdown inclusive end)** — заменено на `sale_dt_filter()` (canonical semi-open). См. line 130.
- **TASK-LEAD-066 (per-SKU drill из popup)** — клик на ряд переходит на `/units?nm_id=X` с `onClose()`. `<Link>` для tab-навигации + tr-onclick для всей строки.
- **TASK-LEAD-067 (PromoCalc 2-col layout)** — `hasResult ? grid-cols-[2fr_3fr] : flex-col` + sticky form.
- **TASK-LEAD-068 (multi-warehouse compare)** — UI отрисовывает таблицу, подсвечивает дешёвый зелёным, считает Δ к текущему складу.
- **TASK-LEAD-071 (SKU-aware picker)** — `productTransitSuggest` endpoint работает, brand-scope check (`if brands is not None and prod.brand not in brands → 403`).
- **TASK-LEAD-072 (Tariff WoW δ)** — корректно использует SCD2 `is_baseline` + sorted timeline, формула per-unit тарифа (`base + extra × per_liter`) совпадает с computeDirectSupply.
- **TASK-LEAD-078 (extension auto-fetch)** — flexible shape parser, `looksLikeTransitTariffs` requires ≥2 rows для anti-false-positive, бэкенд chunked-upsert (CHUNK=1000) c on_conflict_do_update, audit-log запись `wb_transit_tariff.upload`.
- **Localization (PromoCalculator polish)** — «Маржа > 0», «Минимум для окупаемости», «не окупается», «Шт/день» — все термины переведены, англицизмы убраны. Tooltip про «Маржа > 0 vs Δ маржа всего» явный.
- **RBAC для калькуляторов** — `PromoCalculator` через `current_brands_filter` (`api/promo_calculator.py:51`); `TransitCalculator` через `get_db_tenant_scoped` (read открыт всем включая manager, write только director_or_head). Bookkeeper не имеет доступа к меню calculators (Layout whitelist не включает их).
- **`/docs/:slug`** routing работает: backend `doc_pages.py` whitelist, frontend graceful 404 с подсказкой про docker mount.
